#!/usr/bin/python
import os

# register customer
	# there is no api to register customer right now, so we need manually create one

# customer login, get access credential
	# with customer login, password, we can get an access token 
	# with the access token and customer principal, we can generate auth header
# Create restaurant
	# there is no api to create restaurant right now, so we need manually create one
# Create cuisine
	# there is no api to create cuisine right now, so we need manually create one
# Update cuisine info
	# with the auth header and the cuisine info we want to update,
